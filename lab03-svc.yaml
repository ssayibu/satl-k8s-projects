apiVersion: v1
kind: Service
metadata:
  name: lab03-svc
  namespace: labs
spec:
  selector:
    app: lab03
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30155
